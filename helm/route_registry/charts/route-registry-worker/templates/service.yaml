apiVersion: v1
kind: Service
metadata:
  name: {{ include "route-registry-worker.fullname" . }}
  labels:
    {{- include "route-registry-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "route-registry-worker.selectorLabels" . | nindent 4 }}
